var cluster = require('cluster');
var os= require('os');




process.
console.dir(os.totalmem(););
console.dir(os.arch());
console.dir(os.freemem(););
console.dir(os.hostname());
console.dir(os.cpus());
console.dir(os.arch());
console.dir(os.arch());
console.dir(os.arch());
console.dir(os.platform());
console.dir(os.release());